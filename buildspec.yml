version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
  build:
    commands:
      - npm test || true
      - npm run build || true
      - tar -czf backend-app.tar.gz .
artifacts:
  files:
    - backend-app.tar.gz
